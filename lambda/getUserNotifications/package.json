{
  "dependencies": {
    "mongodb": "^7.1.1"
  },
  "scripts": {
    "upload": "aws lambda update-function-code --function-name getUserNotifications --zip-file fileb://getUserNotifications.zip --publish"
  }
}
