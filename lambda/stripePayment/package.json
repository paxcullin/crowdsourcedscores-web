{
  "name": "stripe-payment-lambda",
  "version": "1.0.0",
  "description": "Lambda function to support Stripe payments",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "zip -r stripePayment.zip ."
  },
  "author": "Chris Aronchick",
  "license": "ISC",
  "dependencies": {
    "stripe": "^6.19.0"
  }
}
