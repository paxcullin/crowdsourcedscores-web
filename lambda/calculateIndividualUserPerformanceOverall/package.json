{
  "name": "calculate-individual-user-performance-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "zip -r calculateIndividualUserPerformanceOverall.zip .",
    "upload": "aws lambda update-function-code --function-name calculateIndividualUserPerformanceOverall --zip-file fileb://calculateIndividualUserPerformanceOverall.zip --publish"
  },
  "author": "Chris Aronchick",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.6.10"
  }
}
