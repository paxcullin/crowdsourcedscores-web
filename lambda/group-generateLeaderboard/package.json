{
  "name": "calculateUserScore",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "mongodb": "^4.17.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "zip -r Archive.zip .",
    "upload": "aws lambda update-function-code --function-name group-generateLeaderboard --zip-file fileb://Archive.zip --publish"
  }
}
