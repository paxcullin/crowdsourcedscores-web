{
  "name": "calculate-individual-user-performance-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "aws lambda invoke --function-name calculateUserPerformance --cli-binary-format raw-in-base64-out --payload '{}' calculateUserPerformance-response.json",
    "package": "zip -r calculateUserPerformance.zip .",
    "upload": "aws lambda update-function-code --function-name calculateUserPerformance --zip-file fileb://calculateUserPerformance.zip --publish"
  },
  "author": "Chris Aronchick",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.388.0",
    "mongodb": "^7.1.1"
  }
}
