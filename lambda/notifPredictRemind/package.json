{
  "name": "pushNotifications",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.5.5",
    "expo-server-sdk": "^3.3.0",
    "mongodb": "^4.3.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "zip -r notifPredictRemind.zip .",
    "upload": "aws s3 cp notifPredictRemind.zip s3://shs-lambda-functions/lambda/"
  }
}
