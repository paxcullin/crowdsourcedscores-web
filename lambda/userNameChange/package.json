{
  "name": "get-all-users-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "zip -r userNameChange.zip .",
    "upload": "aws lambda update-function-code --function-name userNameChange --zip-file fileb://userNameChange.zip --publish"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^4.1.1"
  }
}
